{
  "db_name": "PostgreSQL",
  "query": "\n            SELECT\n                login,\n                AVG(score) AS avg_score,\n                SUM(score) AS total_score,\n                COUNT(*) AS flag_count,\n                ARRAY_AGG(DISTINCT reason) AS reasons\n            FROM (\n                SELECT\n                    COALESCE(u.login, uc.login) AS login,\n                    sf.score,\n                    sf.created_at,\n                    unnest(sf.reasons) AS reason\n                FROM spam_flags sf\n                LEFT JOIN issues i ON sf.subject_type = 'issue' AND sf.subject_id = i.id\n                LEFT JOIN users u ON i.user_id = u.id\n                LEFT JOIN comments c ON sf.subject_type = 'comment' AND sf.subject_id = c.id\n                LEFT JOIN users uc ON c.user_id = uc.id\n                WHERE ($1::timestamptz IS NULL OR sf.created_at >= $1)\n            ) flagged\n            WHERE login IS NOT NULL\n            GROUP BY login\n            ORDER BY total_score DESC\n            LIMIT $2\n            ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "login",
        "type_info": "Text"
      },
      {
        "ordinal": 1,
        "name": "avg_score",
        "type_info": "Float8"
      },
      {
        "ordinal": 2,
        "name": "total_score",
        "type_info": "Float4"
      },
      {
        "ordinal": 3,
        "name": "flag_count",
        "type_info": "Int8"
      },
      {
        "ordinal": 4,
        "name": "reasons",
        "type_info": "TextArray"
      }
    ],
    "parameters": {
      "Left": [
        "Timestamptz",
        "Int8"
      ]
    },
    "nullable": [
      null,
      null,
      null,
      null,
      null
    ]
  },
  "hash": "ad2debf4de5763493453930666d1bd004f0324590bab4f03e4eed373f4838050"
}
